//*****예시를 들기위해 작성했습니다*****

// import { ProductEntity } from 'src/common/db/entity/product.entity';
// import { ProductRepository } from './product.repository';

// export class ProductCreateRepository implements ProductCreateOutboundPort {
//   constructor(private readonly productRepository: ProductRepository) {}

//   async execute(object: Record<string, any>): Promise<ProductEntity> {
//     return await this.productRepository.create(object);
//   }
// }
